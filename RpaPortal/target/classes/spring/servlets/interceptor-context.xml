<?xml version="1.0" encoding="UTF-8"?>
<beans:beans  xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">

	<!-- 인터셉터 설정 -->
	<interceptors>
 		<!-- Url 접근 로그 생성 인터셉터 -->
 		<interceptor>
			<mapping path="/**/*.do" />
			<exclude-mapping path="/Account/*.do" />
			<exclude-mapping path="/Common/*.do" />
			<exclude-mapping path="/Ajax*/*.do" />
			<beans:bean class="com.kcc.interceptors.AccessLogInterceptor" />
  		</interceptor>
  		<!-- 디바이스 체크 인터셉터 (스프링 시큐리티 구간은 필터로 체크가 안됨 - AuthenticationProvider) -->
  		<interceptor>
			<mapping path="/**/*.do" />
			<beans:bean class="org.springframework.mobile.device.DeviceResolverHandlerInterceptor" />
		</interceptor>
 	</interceptors>

</beans:beans>